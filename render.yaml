services:
  - type: worker
    name: highrise-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: HIGHRISE_TOKEN
        sync: false
      - key: HIGHRISE_ROOM_ID
        sync: false
